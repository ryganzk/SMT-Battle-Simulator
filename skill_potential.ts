export class SkillPotential {
    phys: number; fire: number; ice: number; elec: number; force: number; light: number; dark: number; almi: number; ailm: number; heal: number; supp: number;

    constructor(phys: number, fire: number, ice: number, elec: number, force: number, light: number, dark: number, almi: number, ailm: number, heal: number, supp: number) {
        this.phys = phys;
        this.fire = fire;
        this.ice = ice;
        this.elec = elec;
        this.force = force;
        this.light = light;
        this.dark = dark;
        this.almi = almi;
        this.ailm = ailm;
        this.heal = heal;
        this.supp = supp;
    }
}